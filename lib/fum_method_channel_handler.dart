import 'package:flutter/services.dart';

class FUMMethodChannelHandler {
  /// native -> flutter
  /// 处理原生消息
  dynamic handlerMethod(MethodCall call) async {
    switch (call.method) {
      // case FUMNToFMethodId.kMapClickedMarkerCallback:

    }
  }
}